{
    "name": "3-custom",  // 全网唯一
    "version": "1.0.0",
    "description": "",
    "main": "index.js",   // 入口文件
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",  //协议：麻省理工的开源——随便用，但要署名
    "dependencies": {
        "lodash": "^4.17.21"
    }
}
------------------------------------------------
上传自己的包
npm adduser 
(注意，这里如果用的是淘宝镜像源会出问题)
     NRM :npm registry manager 是npm的镜像源管理工具
     0 安装nrm
        npm install -g NRM
        nrm ls 查看可选的源 *表示当前源
     1 手工切换源
        1.1 npm config get registry 查看当前源
        1.2 npm config set registry https://registry.npm.taobao.org 切换淘宝源  （ https://registry.npmjs.org）
        1.3 nrm use taobao 切换到taobao源   
npm publish 发布包
npm unpublish --force 卸载包
npm ls 查看当前项目引用了哪些包


export default {
    loginBarShow (page, options) {
        try {
            page.ubtTrace && page.ubtTrace(227565, options);
        } catch (e) {
            // ignore
        }
    },
    loginBarClick (page, options) {
        try {
            page.ubtTrace && page.ubtTrace(227566, options);
        } catch (e) {
            // ignore
        }
    }
};

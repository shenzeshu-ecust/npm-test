<mp-navigation-bar
    wx:if="{{!isQuickApp}}"
    id="mp-navigation"
    color="{{unionVip.moduleSwitch && visible ? '#fff' : '#111111'}}"
    background="{{unionVip.moduleSwitch && visible ? 'linear-gradient(90deg, rgb(93, 93, 93) 0%, rgb(98, 98, 98) 27.51%, rgb(51, 51, 51) 100%)' : '#fff'}}"
    >
    <view slot="center" class="custom-navigation-center">
        <text class="title" style="font-size: 30rpx">携程发票</text>
    </view>
</mp-navigation-bar>
